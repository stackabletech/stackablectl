releaseName: postgresql-airflow
name: postgresql
repo:
  name: bitnami
  url: https://charts.bitnami.com/bitnami/
version: 12.6.6
options:
  auth:
    username: airflow
    password: airflow
    database: airflow
